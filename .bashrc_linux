#
# PROMPT
#

# Define colors
# if tput setaf 1 &> /dev/null; then
# 	tput sgr0; # reset colors
# 	bold=$(tput bold);
# 	reset=$(tput sgr0);
# 	# Solarized colors, taken from http://git.io/solarized-colors.
# 	black=$(tput setaf 0);
# 	blue=$(tput setaf 33);
# 	cyan=$(tput setaf 37);
# 	green=$(tput setaf 64);
# 	orange=$(tput setaf 166);
# 	purple=$(tput setaf 125);
# 	red=$(tput setaf 124);
# 	violet=$(tput setaf 61);
# 	white=$(tput setaf 15);
# 	yellow=$(tput setaf 136);
# else
	bold='';
	reset="\e[0m";
	black="\e[1;30m";
	blue="\e[1;34m";
	cyan="\e[1;36m";
	green="\e[1;32m";
	orange="\e[1;33m";
	purple="\e[1;35m";
	red="\e[1;31m";
	violet="\e[1;35m";
	white="\e[1;37m";
	yellow="\e[1;33m";
# fi;

# Set variable identifying the chroot you work in (used in the prompt below)
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    debian_chroot=$(cat /etc/debian_chroot)
fi

# Set a fancy prompt (non-color, unless we know we "want" color)
case "$TERM" in
    xterm-color|*-256color) color_prompt=yes;;
esac

# Uncomment for a colored prompt, if the terminal has the capability; turned
# off by default to not distract the user: the focus in a terminal window
# should be on the output of commands, not on the prompt
#force_color_prompt=yes

if [ -n "$force_color_prompt" ]; then
    if [ -x /usr/bin/tput ] && tput setaf 1 >&/dev/null; then
	# We have color support; assume it's compliant with Ecma-48
	# (ISO/IEC-6429). (Lack of such support is extremely rare, and such
	# a case would tend to support setf rather than setaf.)
	color_prompt=yes
    else
	color_prompt=
    fi
fi

if [ "$color_prompt" = yes ]; then
    # Default:
    # PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '
    # Two-line prompt:
    # PS1='\[\033[01;32m\]\u\[\033[00m\] at \[\033[01;32m\]\h\[\033[00m\] in \[\033[01;34m\]\w\n\[\033[00m\]\$ '
    # Two-line prompt: (inspired by https://github.com/mathiasbynens/dotfiles)
    PS1="\[\033]0;\W\007\]"; # title: working directory base name
    # PS1+="\[${bold}\]\n"; # newline
    PS1+="\[${green}\]\u"; # username
    PS1+="\[${white}\] at ";
    PS1+="\[${red}\]\h"; # host
    PS1+="\[${white}\] in ";
    PS1+="\[${blue}\]\w"; # working directory full path    
    # PS1+="\$(prompt_git \"\[${white}\] on \[${violet}\]\" \"\[${blue}\]\")"; # Git repository details
    PS1+="\n";
    PS1+="\[${white}\]\$ \[${reset}\]"; # `$` (and reset color)
else
    PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w\$ '
fi
unset color_prompt force_color_prompt

# If this is an xterm set the title to user@host:dir
case "$TERM" in
xterm*|rxvt*)
    PS1="\[\e]0;${debian_chroot:+($debian_chroot)}\u@\h: \w\a\]$PS1"
    ;;
*)
    ;;
esac

# Enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    #alias dir='dir --color=auto'
    #alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

#
# PROGRAMS
#

# EMACS: local load path
# Do this in .emacs instead!
#EMACSLOADPATH=/home/andreas/local/share/site-lisp/
#export EMACSLOADPATH

# LESS
LESS="-M -i -R"
export LESS

# MATLAB: startup path
#MATLAB_USE_USERPATH=/home/andreas/matlab/trunk/
#export MATLAB_USE_USERPATH

# NCL
#export NCARG_ROOT=/home/andreas/local/share/ncl
#export PATH=$PATH:/home/andreas/local/share/ncl/bin

# PYTHON
#export PYTHONPATH=/home/andreas/local/lib/python

# PS2EPS
#export PS2EPS_SIZE=letter # set default paper size

# SVN
export SVN_EDITOR='emacs'

# TEX: enable user contrib TeX stuff
# TEXINPUTS=$TEXINPUTS:/home/andreas/share/texmf//
#TEXINPUTS=/home/andreas/share/texmf//:$TEXINPUTS
TEXINPUTS=/home/andreas/local/share/texmf//:$TEXINPUTS
export TEXINPUTS
  # NOTE that the "//" (double dash) means recursive search through
  # the directory!

# TODO.TXT
if [ -f $HOME/home/andreas/local/etc/bash_completion.d/todo ]; then
    . $HOME/home/andreas/local/etc/bash_completion.d/todo
fi

#
# DEVELOPER
#

export DEBEMAIL="andreasjon@gmail.com"
export DEBFULLNAME="Andreas Jonsson"


# Local Variables:
# mode: sh
# End:
