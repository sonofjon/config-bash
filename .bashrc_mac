### General

## Add /usr/local/sbin to PATH
PATH="${PATH}:/usr/local/sbin"

## Supress zsh new shell warning
export BASH_SILENCE_DEPRECATION_WARNING=1


### Programs

## ActiveTcl
# PATH="/usr/local/bin:${PATH}"

## Base16 shell
BASE16_SHELL="$HOME/git/base16-shell/"
[ -n "$PS1" ] && \
    [ -s "$BASE16_SHELL/profile_helper.sh" ] && \
        eval "$("$BASE16_SHELL/profile_helper.sh")"

## Emacs
if [ -d "/Applications/Emacs.app/Contents/MacOS/bin" ]; then
  PATH="/Applications/Emacs.app/Contents/MacOS/bin:$PATH"
fi

## GNU Core utilities
PATH="/usr/local/opt/grep/libexec/gnubin:${PATH}"

# Homebrew
#   Set PATH, MANPATH, etc.
eval "$(/opt/homebrew/bin/brew shellenv)"

## less
export LESS="-M -i -R"

## MacPorts
# export PATH="$PATH:/opt/local/bin:/opt/local/sbin"

## nvm
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

## Python
PATH="/Library/Frameworks/Python.framework/Versions/3.9/bin:${PATH}"

_pip_completion()
{
    COMPREPLY=( $( COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   PIP_AUTO_COMPLETE=1 $1 2>/dev/null ) )
}
complete -o default -F _pip_completion /usr/local/opt/python@3/bin/python3 -m pip

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/Users/andreas/opt/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/Users/andreas/opt/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/Users/andreas/opt/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/Users/andreas/opt/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

## TeX

# Install destination for tlmgr
export TEXMFHOME="$HOME/.texmf"

